<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>점수 계산 프로그램</title>
</head>
<body>
<script>
  const input = document.getElementById('input').value.toUpperCase();
    let score = 0;
  let plus = 0;

  for (let i = 0; i < input.length; i ++)
      const char = input[i];
    if (char === 'O'){
        plus+=1;
        score+=plus;
    } else if(char === 'P'){
        score += 0;
  } else {
        score +=0;
    }
    document.getElementById('result').innerText(${"총합: "});
</script>

</body>
</html>
